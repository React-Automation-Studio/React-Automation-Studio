import React, { useState } from 'react'
import AutomationStudioContext from './AutomationStudioContext';

const usePV=(props)=>{
  const[pv,setPv]=useState(props.pv);
  
  return(pv)

}
export default usePV